config {
  type: "view",
  database: databases.golden,
  schema: "GLD_OPERACION_EN_SALAS",
  name: "VW_DRAWS_GLD",
  description: "Vista final de draws de las salas de todos los casinos."

}

${salas.lista_salas.map(s => `
  SELECT
    *,
    '${s.nombre}' AS sala,
    ${s.id} AS id_sala
  FROM
    ` + ref(s.esquema, "TBL_DRAWS_STG") + `
`).join("\n UNION ALL \n")}