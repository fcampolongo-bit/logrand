config {
  type: "view",
  database: databases.golden,
  schema: "GLD_BOCA",
  name: "VW_MOBILE_BANKS_GLD",
  description: "Vista consolidada escalable. Actualmente: Jubilee."
}

${salas.lista_salas.map(s => `
  SELECT
    *,
    '${s.nombre}' AS sala,
    ${s.id} AS id_sala
  FROM
    ` + ref(s.esquema, "TBL_MOBILE_BANKS_STG") + `
`).join("\n UNION ALL \n")}